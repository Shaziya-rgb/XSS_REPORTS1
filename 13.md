## Title
Reflected Cross-Site Scripting (XSS) via id Parameter in JavaScript Analytics Context

---
## Vulnerability Type
Reflected XSS

---
## Summary
The `page_id` parameter on the page reflects user input directly inside a JavaScript analytics tracking script without any escaping or sanitization. This means if you break out of the string context with a quote and inject JavaScript, it executes immediately. No storage involved, the payload fires the moment the page loads with the crafted URL.

---
## Vulnerable Endpoint
```
https://kzlabs.com/11.php?page_id=
```
---
## Steps to Reproduce
1. Go to `https://kzlabs.com/11.php` 
2. Submit this as the search term or visit the following URL directly:
```
https://kzlabs.com/13.php?page_id=%27%22%3E%3CIMG%20SRC=X%20onerror=print(1)%3E
```
3. The page loads and a JavaScript alert box pops up displaying `1` confirming the payload broke out of the JS string and executed.

---
## Payload Used
```
"><sCrIpt>alert(1)</sCrIpt>
```

---
## Proof of Concept

**Screenshot 1** — Alert box displaying `1` triggered on the Search Results page after submitting the payload `unique"-alert(1)-"`, with the payload visible in both the URL bar and the search input field confirming reflected XSS execution in the JavaScript context.
<img width="958" height="461" alt="image" src="https://github.com/user-attachments/assets/6a9cd4db-b082-4fb7-bbc0-af304ec69e29" />


## Impact
- An attacker can craft a malicious URL and send it to and the moment they click it the script executes in their browser
- Steal session cookies of anyone who clicks the link giving the attacker full access to their account
- Can be used to redirect victims to fake login pages that look identical to Equifax to harvest credentials
- The vulnerability exists inside an analytics script which shows the dev team never considered it as an injection point making it easy to miss during code review

## Remediation
1. Filter out HTML tags like `<script>`, `<img>`, `<svg>` from the search field before reflecting anything back to the page
2. Filter out JavaScript methods like `alert()`, `confirm()`, `prompt()` so even if a quote slips through the method won't execute
3. Use Cloudflare as they have so many WAF rules that almost all XSS payloads will be blocked automatically before even reaching the server
